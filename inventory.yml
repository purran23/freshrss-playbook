---
all:
  hosts:
  children:
    container:
      hosts:
        freshrss:
          ansible_connection: community.general.incus
          ansible_user: root
          ansible_become: no
          ansible_incus_remote: funbox